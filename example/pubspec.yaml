name: example
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_maps_custom_tooltip:
    path: ../../google_maps_custom_tooltip
  google_maps_flutter: ^2.5.0
  google_maps_flutter_web: ^0.5.4+2

dev_dependencies:
  flutter_lints: ^2.0.3
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

